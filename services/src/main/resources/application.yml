spring:
    application:
        name: service




    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: 
        url: jdbc:mysql://localhost:3306/micro1
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                format_sql: true
        show-sql: true


eureka:
    instance:
        hostname: localhost
